[
  {
    "file": "./caerus/src/ui/detail_pane.rs",
    "line": 937,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let obj = obj.downcast::<PackageObject>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/detail_pane.rs",
    "line": 939,
    "column": 27,
    "text": "downcast::<PackageObject>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let obj = obj.downcast::<PackageObject>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/filter_sidebar.rs",
    "line": 856,
    "column": 103,
    "text": "downcast_ref::<gtk::ListBoxRow>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `// AUTOFIX: Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `let Some(row) = widget.downcast_ref::<gtk::ListBoxRow>() else {``"
  },
  {
    "file": "./caerus/src/ui/filter_sidebar.rs",
    "line": 858,
    "column": 32,
    "text": "downcast_ref::<gtk::ListBoxRow>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `let Some(row) = widget.downcast_ref::<gtk::ListBoxRow>() else {`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 58,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `obj.clone().downcast::<PackageObject>().unwrap()``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 60,
    "column": 17,
    "text": "downcast::<PackageObject>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `obj.clone().downcast::<PackageObject>().unwrap()`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 453,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let cb = item.child().and_downcast::<gtk::CheckButton>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 455,
    "column": 43,
    "text": "downcast::<gtk::CheckButton>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let cb = item.child().and_downcast::<gtk::CheckButton>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 497,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let img = item.child().and_downcast::<gtk::Image>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 499,
    "column": 40,
    "text": "downcast::<gtk::Image>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let img = item.child().and_downcast::<gtk::Image>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 564,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 566,
    "column": 38,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 598,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 600,
    "column": 38,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 616,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 618,
    "column": 34,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 641,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 643,
    "column": 34,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 665,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 667,
    "column": 34,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 682,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 684,
    "column": 34,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 114,
    "column": 128,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `// AUTOFIX: Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_setup(move |_, item| setup(item.downcast_ref::<gtk::ListItem>().unwrap()));``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 116,
    "column": 53,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_setup(move |_, item| setup(item.downcast_ref::<gtk::ListItem>().unwrap()));`"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 117,
    "column": 126,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `// AUTOFIX: Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_bind(move |_, item| bind(item.downcast_ref::<gtk::ListItem>().unwrap()));``"
  },
  {
    "file": "./caerus/src/ui/package_list.rs",
    "line": 119,
    "column": 51,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_bind(move |_, item| bind(item.downcast_ref::<gtk::ListItem>().unwrap()));`"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 57,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `obj.clone().downcast::<KernelObject>().unwrap()``"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 59,
    "column": 17,
    "text": "downcast::<KernelObject>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `obj.clone().downcast::<KernelObject>().unwrap()`"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 170,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let cb = item.child().and_downcast::<gtk::CheckButton>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 172,
    "column": 39,
    "text": "downcast::<gtk::CheckButton>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let cb = item.child().and_downcast::<gtk::CheckButton>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 189,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();``"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 191,
    "column": 38,
    "text": "downcast::<gtk::Label>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `let l = item.child().and_downcast::<gtk::Label>().unwrap();`"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 89,
    "column": 128,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `// AUTOFIX: Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_setup(move |_, item| setup(item.downcast_ref::<gtk::ListItem>().unwrap()));``"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 91,
    "column": 53,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_setup(move |_, item| setup(item.downcast_ref::<gtk::ListItem>().unwrap()));`"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 92,
    "column": 126,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `// AUTOFIX: Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_bind(move |_, item| bind(item.downcast_ref::<gtk::ListItem>().unwrap()));``"
  },
  {
    "file": "./caerus/src/ui/vkpurge_dialog.rs",
    "line": 94,
    "column": 51,
    "text": "downcast_ref::<gtk::ListItem>()",
    "pattern": "downcast_ref",
    "suggestion": "Verify type before using `downcast_ref`, avoid `unwrap()`. Found: `factory.connect_bind(move |_, item| bind(item.downcast_ref::<gtk::ListItem>().unwrap()));`"
  },
  {
    "file": "./caerus/src/backend/package_store.rs",
    "line": 748,
    "column": 195,
    "text": "pkgname).unwrap()",
    "pattern": "unwrap",
    "suggestion": "Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `// AUTOFIX: Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `let p = ht.get_mut(&pkgname).unwrap();``"
  },
  {
    "file": "./caerus/src/backend/package_store.rs",
    "line": 750,
    "column": 25,
    "text": "pkgname).unwrap()",
    "pattern": "unwrap",
    "suggestion": "Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `let p = ht.get_mut(&pkgname).unwrap();`"
  },
  {
    "file": "./tools/autofix/src/main.rs",
    "line": 61,
    "column": 190,
    "text": "0).unwrap()",
    "pattern": "unwrap",
    "suggestion": "Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `// AUTOFIX: Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `let m0 = m.get(0).unwrap();``"
  },
  {
    "file": "./tools/autofix/src/main.rs",
    "line": 63,
    "column": 36,
    "text": "0).unwrap()",
    "pattern": "unwrap",
    "suggestion": "Consider replacing `.unwrap()` with `match ... { Some(x) => x, None => { eprintln!(\\\"...\\\"); return; } }` or `if let Some(x) = ...` depending on context. Found: `let m0 = m.get(0).unwrap();`"
  },
  {
    "file": "./tools/autofix/src/main.rs",
    "line": 157,
    "column": 22,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `// AUTOFIX: Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `\"downcast_unwrap\" => format!(\"Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `{}`\", line.trim()),``"
  },
  {
    "file": "./tools/autofix/src/main.rs",
    "line": 159,
    "column": 48,
    "text": "downcast::<T>().unwrap()",
    "pattern": "downcast_unwrap",
    "suggestion": "Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `\"downcast_unwrap\" => format!(\"Replace `downcast::<T>().unwrap()` with a safe match or downcast_ref and log on failure. Found: `{}`\", line.trim()),`"
  }
]